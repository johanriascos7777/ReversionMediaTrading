{
  "name": "elasticity-backend",
  "version": "1.0.0",
  "description": "Backend WebSocket para Elasticity System — EUR/USD",
  "main": "dist/server.js",
  "scripts": {
    "dev": "ts-node-dev --respawn --transpile-only src/server.ts",
    "build": "npm install && tsc",
    "start": "node dist/server.js"
  },
  "dependencies": {
    "ws": "^8.16.0",
    "@types/node": "^20.0.0",
    "@types/ws": "^8.5.10",
    "typescript": "^6.0.0"
  },
  "devDependencies": {
    "ts-node-dev": "^2.0.0"
  }
}